# scriptable widget - get current public ip 

This widget was adapted from the Scriptabe template "Random Scriptable API". 

It is a simple widget that displays the current public IP (v4), country and provider name.